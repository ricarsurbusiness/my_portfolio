// @ts-check
import { defineConfig } from "astro/config";

// https://astro.build/config
export default defineConfig({
  site: "https://ricarsurbusiness.github.io",
  base: "/portfolio_rs",
  output: "static",
});
